head
  meta(charset='UTF-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title Documento
body
  table 
    thead
      tr
        th= "Title"
        th="Image"
        th="price"
      if productosArray.length > 0
        each book in productosArray
          tr 
            td= book.title
            td
             img(src=book.thumbnail alt="" style="max-height: 150px")
            td=book.price
      else 
        tr
          td="No hay libros"

  button
    a(href='/static') Ir a agregar produto

